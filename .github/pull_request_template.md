Closes #N
<!-- `Fixes #N` / `Resolves #N` and several linked issues are also accepted; a keyword inside backticks or a fenced code block is ignored. -->

## What changed

## Proof
Test command, counts, duration. Paste the summary, not the whole log.

## Files
Globs touched (must match the issue).
<!-- authorised: `glob` — orchestrator-only; a grant here counts only inside this ## Files section. -->

## Risks
